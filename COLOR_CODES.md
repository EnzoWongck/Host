# 🎨 應用程式顏色代碼清單

## 📋 淺色模式 (Light Mode)

| 顏色名稱 | 顏色代碼 | 用途說明 |
|---------|---------|---------|
| **primary** | `#E2E8F0` | 深灰按鈕背景色 |
| **secondary** | `#8B5CF6` | 次要顏色（紫色） |
| **background** | `#FFFFFF` | 主背景色（白色） |
| **surface** | `#F8FAFC` | 卡片/表面背景色（淺灰） |
| **text** | `#1E293B` | 主要文字顏色（深灰） |
| **textSecondary** | `#64748B` | 次要文字顏色（中灰） |
| **border** | `#E2E8F0` | 邊框顏色（淺灰） |
| **success** | `#10B981` | 成功/正數顏色（綠色） |
| **error** | `#EF4444` | 錯誤/負數顏色（紅色） |
| **warning** | `#F59E0B` | 警告顏色（橙色） |
| **info** | `#3B82F6` | 資訊顏色（藍色） |

---

## 🌙 深色模式 (Dark Mode)

| 顏色名稱 | 顏色代碼 | 用途說明 |
|---------|---------|---------|
| **primary** | `#303134` | 深灰按鈕背景色 |
| **secondary** | `#303134` | 次要顏色（深灰） |
| **background** | `#1A1A1A` | 主背景色（深灰色） |
| **surface** | `#202124` | 卡片/表面背景色（比背景稍亮） |
| **text** | `#FFFFFF` | 主要文字顏色（白色） |
| **textSecondary** | `#D1D5DB` | 次要文字顏色（淺灰） |
| **border** | `#3A3A3A` | 邊框顏色（比surface稍亮） |
| **success** | `#FFFFFF` | 成功/正數顏色（白色，替代綠色） |
| **error** | `#EF4444` | 錯誤/負數顏色（紅色） |
| **warning** | `#F59E0B` | 警告顏色（橙色） |
| **info** | `#6B7280` | 資訊顏色（灰色） |

---

## 🎯 特殊用途顏色

### 輸入框背景色（淺色模式）
- **輸入框背景**: `#F8F9FA`

### 選中狀態顏色（淺色模式）
- **選中邊框**: `#E2E8F0`
- **選中背景**: `#E2E8F0`

### 按鈕文字顏色
- **主要按鈕文字（淺色模式）**: `#64748B`
- **主要按鈕文字（深色模式）**: `#FFFFFF`
- **危險按鈕文字**: `#FFFFFF`
- **次要按鈕文字**: 背景色（反色）

---

## 📊 顏色對比表

### 淺色模式顏色預覽

```
primary:      #E2E8F0  ████  (淺灰藍)
secondary:    #8B5CF6  ████  (紫色)
background:   #FFFFFF  ████  (白色)
surface:      #F8FAFC  ████  (極淺灰)
text:         #1E293B  ████  (深灰)
textSecondary:#64748B  ████  (中灰)
border:       #E2E8F0  ████  (淺灰藍)
success:      #10B981  ████  (綠色)
error:        #EF4444  ████  (紅色)
warning:      #F59E0B  ████  (橙色)
info:         #3B82F6  ████  (藍色)
```

### 深色模式顏色預覽

```
primary:      #303134  ████  (深灰)
secondary:    #303134  ████  (深灰)
background:   #1A1A1A  ████  (極深灰)
surface:      #202124  ████  (深灰)
text:         #FFFFFF  ████  (白色)
textSecondary:#D1D5DB  ████  (淺灰)
border:       #3A3A3A  ████  (中灰)
success:      #FFFFFF  ████  (白色)
error:        #EF4444  ████  (紅色)
warning:      #F59E0B  ████  (橙色)
info:         #6B7280  ████  (灰色)
```

---

## 🔍 使用範例

### 在組件中使用顏色

```typescript
import { useTheme } from '../context/ThemeContext';

const MyComponent = () => {
  const { theme, colorMode } = useTheme();
  
  return (
    <View style={{ backgroundColor: theme.colors.background }}>
      <Text style={{ color: theme.colors.text }}>
        主要文字
      </Text>
      <Text style={{ color: theme.colors.textSecondary }}>
        次要文字
      </Text>
      <View style={{ 
        backgroundColor: theme.colors.primary,
        borderColor: theme.colors.border 
      }}>
        按鈕
      </View>
    </View>
  );
};
```

### 條件顏色（根據模式）

```typescript
const { theme, colorMode } = useTheme();

const backgroundColor = colorMode === 'light' 
  ? '#F8F9FA'  // 淺色模式
  : theme.colors.background;  // 深色模式
```

---

## 📝 備註

- 所有顏色代碼使用 **HEX 格式**（#RRGGBB）
- 顏色定義在 `src/types/theme.ts` 文件中
- 使用 `useTheme()` hook 來獲取當前主題顏色
- 顏色會根據 `colorMode`（'light' 或 'dark'）自動切換

---

**最後更新**: 2025-11-30

