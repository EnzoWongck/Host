# 🔥 Firebase DNS 配置指南 - Vercel DNS

## 📋 Firebase 要求的 DNS 記錄

### ✅ 需要添加的記錄：

1. **A 記錄**
   - 記錄類型：A
   - 網域名稱：host27o.com
   - 值：199.36.158.100

2. **TXT 記錄**
   - 記錄類型：TXT
   - 網域名稱：host27o.com
   - 值：hosting-site=host-7b3ce

### ❌ 需要移除的記錄：

1. **A 記錄**
   - 記錄類型：A
   - 網域名稱：host27o.com
   - 值：64.29.17.1

2. **A 記錄**
   - 記錄類型：A
   - 網域名稱：host27o.com
   - 值：64.29.17.65

---

## 🚀 步驟 1：在 Vercel Dashboard 移除舊的 A 記錄

### 1.1 登入 Vercel Dashboard

1. 前往 [Vercel Dashboard](https://vercel.com/dashboard)
2. 使用您的帳號登入

### 1.2 進入域名設置

1. 選擇專案 **host27o**（或您的專案名稱）
2. 點擊 **Settings**（設置）
3. 在左側選單中，點擊 **Domains**（域名）

### 1.3 管理 DNS 記錄

1. 找到域名 `host27o.com`
2. 點擊域名旁邊的 **...**（三個點）或 **Manage**（管理）
3. 選擇 **DNS Records**（DNS 記錄）或 **DNS Configuration**（DNS 配置）

### 1.4 移除舊的 A 記錄

找到並刪除以下兩個 A 記錄：
- `host27o.com` → `64.29.17.1`
- `host27o.com` → `64.29.17.65`

點擊每個記錄旁邊的 **刪除** 圖標（垃圾桶 🗑️）

---

## ✅ 步驟 2：在 Vercel Dashboard 添加新的 DNS 記錄

### 2.1 添加 A 記錄

1. 在 DNS 記錄頁面，點擊 **Add Record**（添加記錄）或 **+** 按鈕
2. 選擇 **Type**: A
3. **Name**: `@` 或留空（表示根域名）
4. **Value**: `199.36.158.100`
5. **TTL**: 3600（或自動）
6. 點擊 **Save**（保存）

### 2.2 添加 TXT 記錄

1. 再次點擊 **Add Record**（添加記錄）
2. 選擇 **Type**: TXT
3. **Name**: `@` 或留空（表示根域名）
4. **Value**: `hosting-site=host-7b3ce`
5. **TTL**: 3600（或自動）
6. 點擊 **Save**（保存）

---

## 🔍 步驟 3：驗證 DNS 記錄

等待 5-10 分鐘讓 DNS 傳播後，驗證記錄：

### 檢查 A 記錄
```powershell
# Windows PowerShell
nslookup host27o.com
```

應該看到：`199.36.158.100`

### 檢查 TXT 記錄
```powershell
# Windows PowerShell
nslookup -type=TXT host27o.com
```

應該看到：`hosting-site=host-7b3ce`

---

## 📊 完整的 DNS 記錄配置

完成後，您的 DNS 記錄應該如下：

```
記錄類型    網域名稱        值
A           host27o.com    199.36.158.100
TXT         host27o.com    hosting-site=host-7b3ce
```

**不應該有**：
- ❌ `64.29.17.1`
- ❌ `64.29.17.65`
- ❌ `216.198.79.1`
- ❌ `216.198.79.65`

---

## ⏱️ 步驟 4：等待 Firebase 驗證

1. 返回 [Firebase Console](https://console.firebase.google.com/)
2. 前往 **Hosting** > **添加自定義域**
3. Firebase 會自動檢測 DNS 記錄
4. 驗證過程通常需要 **5-30 分鐘**

### 驗證狀態

在 Firebase Console 中，您會看到：
- ⏳ **Pending** - 等待驗證（正常，請等待）
- ✅ **Active** - 驗證成功
- ❌ **Error** - 需要檢查 DNS 記錄

---

## 🔐 步驟 5：配置授權域（用於 Google 登入）

為了讓 Google 登入顯示 "繼續使用 host27o.com"：

1. 在 Firebase Console，前往 **Authentication** > **Settings** > **授權域**
2. 點擊 **添加域**
3. 輸入：`host27o.com`
4. 點擊 **添加**

---

## 🚀 步驟 6：部署到 Firebase Hosting

DNS 配置完成後，部署應用：

```bash
# 構建應用
npx expo export --platform web

# 部署到 Firebase Hosting
firebase deploy --only hosting
```

---

## 🔍 常見問題

### 問題 1：無法在 Vercel 找到 DNS 記錄設置

**可能原因**：
- 域名可能使用外部 DNS 供應商

**解決方法**：
1. 檢查域名註冊商（如 GoDaddy、Namecheap）
2. 在註冊商的 DNS 管理頁面添加記錄

### 問題 2：Firebase 無法驗證域名

**可能原因**：
- DNS 記錄尚未傳播
- 記錄值輸入錯誤

**解決方法**：
1. 等待 10-30 分鐘
2. 使用 `nslookup` 驗證記錄是否正確
3. 檢查 TXT 記錄值是否完全匹配（包括 `hosting-site=` 前綴）

### 問題 3：TXT 記錄添加失敗

**可能原因**：
- Vercel 可能不支援 TXT 記錄（較少見）

**解決方法**：
1. 如果 Vercel 不支援，需要在域名註冊商處添加 TXT 記錄
2. 保持 A 記錄在 Vercel，TXT 記錄在註冊商

---

## 📝 如果使用外部 DNS 供應商

如果您的域名實際上使用外部 DNS 供應商（不是 Vercel DNS），請在註冊商處配置：

### GoDaddy
1. 登入 [GoDaddy](https://www.godaddy.com)
2. **My Products** > **Domains** > 找到 `host27o.com` > **DNS**
3. 移除舊的 A 記錄
4. 添加新的 A 記錄：`@` → `199.36.158.100`
5. 添加 TXT 記錄：`@` → `hosting-site=host-7b3ce`

### Namecheap
1. 登入 [Namecheap](https://www.namecheap.com)
2. **Domain List** > 找到 `host27o.com` > **Manage** > **Advanced DNS**
3. 移除舊的 A 記錄
4. 添加新的 A 記錄：`@` → `199.36.158.100`
5. 添加 TXT 記錄：`@` → `hosting-site=host-7b3ce`

### Cloudflare
1. 登入 [Cloudflare](https://dash.cloudflare.com)
2. 選擇域名 `host27o.com` > **DNS** > **Records**
3. 移除舊的 A 記錄
4. 添加新的 A 記錄：`@` → `199.36.158.100`（DNS only，灰色雲朵）
5. 添加 TXT 記錄：`@` → `hosting-site=host-7b3ce`

---

## ✅ 完成檢查清單

- [ ] 移除 A 記錄：`64.29.17.1`
- [ ] 移除 A 記錄：`64.29.17.65`
- [ ] 添加 A 記錄：`199.36.158.100`
- [ ] 添加 TXT 記錄：`hosting-site=host-7b3ce`
- [ ] 等待 5-10 分鐘讓 DNS 傳播
- [ ] 驗證 DNS 記錄（使用 nslookup）
- [ ] 返回 Firebase Console 檢查驗證狀態
- [ ] 配置授權域（Authentication > Settings > 授權域）
- [ ] 部署到 Firebase Hosting

---

## 🎉 完成！

配置完成後：
1. ✅ 域名指向 Firebase Hosting
2. ✅ Firebase 驗證域名擁有權
3. ✅ SSL 證書自動配置
4. ✅ Google 登入顯示 "繼續使用 host27o.com"
5. ✅ 網站可通過 `https://host27o.com` 訪問

**祝配置順利！🎊**

