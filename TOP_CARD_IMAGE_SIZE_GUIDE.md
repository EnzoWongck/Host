# 頂部卡片圖片尺寸指南

## 當前卡片尺寸計算

### 卡片寬度
- 屏幕寬度 - 左右 padding（`theme.spacing.lg` × 2 = 48px）
- 常見手機屏幕（375px）：卡片寬度 = 375 - 48 = **327px**
- 常見手機屏幕（414px）：卡片寬度 = 414 - 48 = **366px**
- 平板屏幕（768px）：卡片寬度 = 768 - 48 = **720px**

### 卡片高度
- `minHeight: 100px` + `padding: lg` (24px) × 2 = **約 148px**
- 實際高度由內容決定，約 **100-150px**

### 寬高比建議
- **手機（375-414px 寬）**：約 **2.2:1 到 2.5:1**
  - 推薦尺寸：**750×300px** 或 **800×350px**
- **平板（768px 寬）**：約 **4.8:1 到 5:1**
  - 推薦尺寸：**2400×500px** 或 **3000×600px**

## 推薦圖片尺寸

### 最佳選擇（適配多種設備）
- **寬度：1200-1500px**
- **高度：300-400px**
- **寬高比：約 3:1 到 4:1**
- **格式：PNG（支持透明）或 JPG**

### 具體建議
1. **手機優先**：**900×300px**（3:1 比例）
2. **平板優先**：**1500×400px**（3.75:1 比例）
3. **通用方案**：**1200×350px**（約 3.4:1 比例）

## 當前設置
- `resizeMode="contain"`：按比例完整顯示，可能留白
- 如需填滿：改為 `resizeMode="cover"`，但可能裁剪部分圖片

## 建議
使用 **1200×350px** 的圖片，寬高比約 **3.4:1**，可以：
- 在手機上完整顯示（可能上下留白）
- 在平板上完整顯示（可能左右留白）
- 使用 `resizeMode="contain"` 確保圖片完整顯示











