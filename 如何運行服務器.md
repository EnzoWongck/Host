# 📱 如何運行服務器（手機訪問）

## 🚀 方法 1：使用隧道模式（推薦，最簡單）

### 步驟：
1. **雙擊運行** `start-web-tunnel.bat` 文件
2. 等待服務器啟動（約 30-60 秒）
3. 終端會顯示：
   - QR 碼（用於 Expo Go）
   - Web URL（用於手機瀏覽器）
   - 類似：`exp://xxx.xxx.xxx.xxx:8081`
4. **在手機上：**
   - **Expo Go**：打開 Expo Go app，掃描 QR 碼
   - **瀏覽器**：輸入終端顯示的 Web URL

### 優點：
- ✅ 不需要配置防火牆
- ✅ 可以穿透網絡限制
- ✅ 適用於任何網絡環境

---

## 🌐 方法 2：使用 LAN 模式

### 步驟：
1. **雙擊運行** `start-web-lan.bat` 文件
2. 等待服務器啟動
3. 終端會顯示：
   - 你的電腦 IP 地址
   - 端口號（通常是 19000）
4. **在手機瀏覽器中輸入：**
   ```
   http://192.168.79.6:19000
   ```
   （IP 地址請使用終端顯示的實際 IP）

### 注意事項：
- ⚠️ 需要手機和電腦在同一 Wi-Fi 網絡
- ⚠️ 可能需要配置 Windows 防火牆

---

## 🔧 方法 3：手動在命令提示符中運行

### 打開命令提示符（cmd）：
1. 按 `Win + R`
2. 輸入 `cmd`
3. 按 Enter

### 切換到項目目錄：
```cmd
cd C:\Users\user\Documents\GitHub\Host
```

### 運行命令：

**隧道模式：**
```cmd
npx expo start --web --tunnel
```

**LAN 模式：**
```cmd
npx expo start --web --host lan
```

---

## 📋 快速對比

| 方法 | 優點 | 缺點 | 適用場景 |
|------|------|------|----------|
| 隧道模式 | 無需配置，穿透防火牆 | 啟動較慢 | 任何網絡環境 |
| LAN 模式 | 速度快 | 需要同 Wi-Fi，可能被防火牆阻止 | 家庭/辦公室網絡 |

---

## 🎯 推薦流程

1. **首先嘗試**：雙擊 `start-web-tunnel.bat`（最簡單）
2. **如果隧道模式太慢**：嘗試 `start-web-lan.bat`
3. **如果 LAN 模式無法訪問**：檢查防火牆設置

---

## ❓ 常見問題

### Q: 雙擊 .bat 文件沒有反應？
A: 右鍵點擊文件，選擇「以管理員身份運行」

### Q: 顯示「無法找到 npm」？
A: 確保已安裝 Node.js，或使用命令提示符手動運行

### Q: 服務器啟動很慢？
A: 隧道模式需要連接到 Expo 服務器，可能需要 1-2 分鐘

### Q: 手機掃描 QR 碼無法連接？
A: 確保手機和電腦都連接到互聯網（隧道模式需要）

---

## 💡 提示

- 服務器啟動後，**不要關閉終端窗口**
- 修改代碼後，應用會自動重新加載
- 按 `Ctrl + C` 可以停止服務器












